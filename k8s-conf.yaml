apiVersion: v1
kind: Service
metadata:
  name: conftest-devel-maiamcc-lb-blorgfrontend
  labels:
    app: blorg
    owner: maiamcc
    environment: devel
    tier: backend
spec:
  type: LoadBalancer
  ports:
  - port: 8080
    targetPort: 8080
  selector:
    app: blorg
    owner: maiamcc
    environment: devel
    tier: backend
---
apiVersion: apps/v1beta2
kind: Deployment
metadata:
  name: conftest-devel-maiamcc-blorgfrontend
spec:
  selector:
    matchLabels:
      app: blorg
      owner: maiamcc
      environment: devel
      tier: backend
  template:
    metadata:
      name: conftest-devel-maiamcc-blorgfrontend
      labels:
        app: blorg
        owner: maiamcc
        environment: devel
        tier: backend
    spec:
      containers:
      - name: frontend
      # TODO(maia): flag for backend server IP
        image: gcr.io/blorg-dev/blorg-frontend
        ports:
        - containerPort: 8080